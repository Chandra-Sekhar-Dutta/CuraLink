module.exports = {
  plugins: {
    // Use the official tailwindcss PostCSS plugin so the
    // `@tailwind` directives in `globals.css` are compiled.
    tailwindcss: {},
    autoprefixer: {},
  },
};